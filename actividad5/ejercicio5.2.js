// Construye un código bajo las siguientes reglas
//  *Declara un arreglo vacío *Con un ciclo While que se ejecute 5 veces, agrega los 
//   elementos al array

var arreglo = [prompt("ingresa un nombre")]
var i = 0
while (i == arreglo){
    if(i >= 5)
    console.log(arreglo.length[i])
}
i++